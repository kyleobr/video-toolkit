from setuptools import setup, find_packages

setup(
    name="video-toolkit",
    version="0.2.1",
    packages=find_packages(),
    python_requires=">=3.7",
    description="Lightweight video metadata extraction and thumbnail generation",
    author="kyleobr",
    license="MIT",
)
